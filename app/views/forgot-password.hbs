<div class="row" ng-controller="ForgotPasswordController">
  <div class="large-12 columns">
    <form name="forgotPasswordForm" class="register-form" novalidate ng-submit="submit()">
      <div class="row">
        <div class="large-8 large-centered columns">
          <h1>ลืมรหัสผ่าน</h1>
        </div>
      </div>

      <div class="row">
        <div class="large-8 large-centered columns">

          <div data-alert class="alert-box alert radius" ng-show="error" ng-cloak>
            เกิดข้อผิดพลาด กรุณาลองใหม่อีกครั้ง
          </div>

          <div data-alert class="alert-box success radius" ng-show="message" ng-cloak>
            [[ message ]]
          </div>

          <div data-alert class="alert-box alert radius" ng-show="invalidResetLink" ng-cloak>
            ไม่ได้สามารถตั้งค่ารหัสผ่านใหม่ได้ โปรดตรวจสอบลิงก์ใหม่
          </div>

          <div class="row">
            <div class="large-3 columns">
              <label class="right-label inline">อีเมล</label>
            </div>
            <div class="large-9 columns">
              <input type="email" required ng-model="email" name="email" placeholder="mayom@sicksense.org" class="no-margin" />
              <small class="error" ng-show="invalidEmail" ng-cloak>กรุณากรอกอีเมลให้ถูกต้อง</small>
              <small class="error" ng-show="invalidUser" ng-cloak>ไม่พบอีเมลในระบบ</small>
            </div>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="large-8 large-centered columns">
          <input type="submit" class="button ss-button ss-button-full-width" ng-class="{disabled:submitting}" value="ยืนยัน" />
        </div>
      </div>
    </form>
  </div>
</div>

{{#content "js_footer"}}
<script src="/js/app-forgotpassword.js"></script>
{{/content}}
