<div class="row" ng-controller="ResetPasswordController">
  <div ng-init="initial()"></div>

  <div class="large-8 columns large-centered" ng-show="submitStatus == 'completed'" ng-cloak>
    <div class="panel radius container-block">
        <h2 class="text-center">ตั้งค่ารหัสผ่านใหม่</h2>
        <div class="text-center">
          <p>ตั้งรหัสผ่านใหม่เรียบร้อยแล้ว คุณสามารถใช้รหัสผ่านใหม่ในการเข้าสู่ระบบได้ทันที</p>
          <a ng-show="isiOS" class="button ss-button radius" href="sicksense://finish-action" ng-cloak><span class="doctorme-icon"></span>เข้าใช้งานแอบพลิเคชั่นหมอชาวบ้าน</a>
          <a ng-hide="isiOS" class="button ss-button radius" href="/login.html" ng-cloak>เข้าสู่ระบบ</a>
        </div>
    </div>
  </div>

  <div class="large-12 columns" ng-hide="submitting && submitStatus == 'completed'" ng-cloak>
    <form class="reset-password-form" ng-submit="submit()">
      <div class="row">
        <div class="large-8 large-centered columns">
          <h1>ตั้งค่ารหัสผ่านใหม่</h1>
        </div>
      </div>

      <div class="row">
            <div class="large-8 large-centered columns">
                <!-- ALERT COMPLETED -->
                <div data-alert class=" alert-box success radius" ng-show="submitStatus == 'completed'" ng-cloak>
                  ตั้งรหัสผ่านใหม่เรียบร้อยแล้ว กรุณา<a href="login.html">เข้าสู่ระบบ</a>
                </div>

                <!-- ALERT FAILED -->
                <div data-alert class=" alert-box alert radius" ng-show="submitStatus == 'failed'" ng-cloak>
                  เกิดข้อผิดพลาด กรุณาลองใหม่อีกครั้ง
                </div>
            </div>
      </div>

      <div class="row">
        <div class="large-8 large-centered columns">
          <div class="row">
            <div class="large-3 columns">
              <label class="right-label inline">รหัสผ่าน</label>
            </div>
            <div class="large-9 columns">
              <input type="password" ng-model="password" placeholder="********" class="no-margin"/>
              <small class="error" ng-show="invalidPassword" ng-cloak>รหัสผ่านต้องไม่น้อยกว่า 8 ตัวอักษร และไม่มากกว่า 64 ตัวอักษร</small>
            </div>
          </div>
        </div>
      </div>
      <div class="row">
        <div class="large-8 large-centered columns">
          <div class="row">
            <div class="large-3 columns">
              <label class="right-label inline">ยืนยันรหัสผ่าน</label>
            </div>
            <div class="large-9 columns">
              <input type="password" ng-model="repassword" placeholder="********" class="no-margin"/>
              <small class="error" ng-show="invalidSamePassword" ng-cloak>รหัสผ่านไม่ตรงกัน</small>
            </div>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="large-8 large-centered columns">
          <input type="submit" class="button ss-button ss-button-full-width" value="ยืนยัน" />
        </div>
      </div>
    </form>
  </div>
</div>


{{#content "js_footer"}}
<script src="/bower_components/jquery.browser/dist/jquery.browser.min.js"></script>
<script src="/js/app-reset-password.js"></script>
{{/content}}
